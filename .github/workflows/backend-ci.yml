name: backend-ci

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    paths:
      - packages/backend/**
      - .github/workflows/backend-ci.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend-lint:
    name: backend-lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/backend

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
          prune-cache: true
          working-directory: packages/backend
          uv-version: "latest"

      - name: Create venv
        run: uv venv

      - name: Install dependencies
        run: uv pip install -e ".[dev]"

      - name: Ruff lint
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format . --check

      - name: Check Alembic has single head
        run: |
          heads="$(uv run alembic heads)"
          count="$(printf '%s\n' "$heads" | sed '/^[[:space:]]*$/d' | wc -l)"
          if [ "$count" -ne 1 ]; then
            echo "Expected exactly one Alembic head, but found $count:" >&2
            printf '%s\n' "$heads" >&2
            exit 1
          fi

  backend-test:
    name: backend-test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/backend

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: tasche_test
          POSTGRES_USER: tasche
          POSTGRES_PASSWORD: tasche_dev_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U tasche -d tasche_test" --health-interval
          10s --health-timeout 5s --health-retries 10

    env:
      DATABASE_URL: postgresql+asyncpg://tasche:tasche_dev_password@localhost:5432/tasche_test
      TEST_DATABASE_URL: postgresql+asyncpg://tasche:tasche_dev_password@localhost:5432/tasche_test
      # TCH-75: JWT 方式廃止に伴い JWT_SECRET/TEST_JWT_SECRET を削除。
      # stub-login エンドポイントを有効化するために AUTH_STUB_ENABLED を設定。
      AUTH_STUB_ENABLED: "true"
      ENABLE_TEST_AUTH: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
          prune-cache: true
          working-directory: packages/backend
          uv-version: "latest"

      - name: Create venv
        run: uv venv

      - name: Install dependencies
        run: uv pip install -e ".[dev]"

      - name: Wait for Postgres
        env:
          PGPASSWORD: tasche_dev_password
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client
          for i in {1..60}; do
            pg_isready -h localhost -p 5432 -U tasche -d tasche_test && exit 0
            sleep 1
          done
          echo "Postgres did not become ready in time" >&2
          exit 1

      - name: Run tests
        run: timeout 10m uv run pytest
